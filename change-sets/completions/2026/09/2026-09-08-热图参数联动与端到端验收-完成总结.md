---
document_type: change_set_completion
change_set_id: CS-20260908-5791da7b-75fa-4fcf-9223-ad84065b80a5
related_change_set_ids:
  - CS-20260901-6c70c1b0-5ed0-4e44-9c4d-2f21f5431b4f
  - CS-20260904-052650cd-885e-4f3c-937f-9da170327883
outcome: completed
closed_at: 2026-09-08T19:48:25+08:00
---

# 热图参数联动与端到端验收完成总结

## 结果摘要

热图参数和 Tab 显隐、值标记最多两列已实现；完整关联回归 146 项及连续 Playwright 20 个交互场景全部通过。前端变更已提交，本文档与计划由文档仓库当前归档提交收录。

## 原因与目标

根据真实热图模板及数据字典实现用户列出的显隐规则，覆盖初次加载、条件切换、文件标记保存、刷新回显和对称性改变；补跑此前受 MySQL 连接超时阻断的跨图组回归。

## 设计决策

扩展既有热图刷新入口，复用公共布尔解析、文件标记和注释来源逻辑；避免在各 Vue 控件中重复判断。两列上限仅作用于热图 value，统一拦截新增选择并在保存前校验历史超限标记，不静默删列。数据库模板仅只读核对。

## 实际变更

- 热图刷新逻辑处理单元格 A/B、聚类、标签及条件撤销；保留用户原有注释条修复和非对称限制。
- 列标记 mixin、选择弹窗和保存入口实施两列限制；增加真实字段 fixture、单元/集成/组件方法回归。
- 本轮追加验收仅修改验证文档，未修改产品代码、后端、依赖、共享模板、R 绘图算法或其他用户已有变更。

## 验证结果

- 完整命令及覆盖项见关联实施记录：12 个测试套件、146 项通过，退出码 0；包含此前未完成的生命周期契约测试，未跳过测试或替换真实字典。
- Playwright：重新登录本地 home/local 后端，上传合成数据集和行列属性文件；在本次绘图 `355665843968802816` 上通过页面交互连续执行 20 场景，全部通过。
- 覆盖两列上限、两个原始值开关、三个聚类开关、六种样式、双侧注释、单值 split、非对称双值、保存刷新及恢复对称。最终 trace 无动作/页面控制台错误；42 次本地代理 API 请求无 HTTP 4xx/5xx。
- 前端编译成功，有既有 warnings；本轮未改 Java，使用既有 JAR 启动验证，未重新执行 Maven。未执行 R 分析与图像导出，因不属于本次参数标记需求。
- 登录页已恢复原始 SHA-256，Git 已暂存/未暂存差异为空。受控终端 Ctrl-C 关闭前端 41562、后端 42268，监听检查确认端口释放。

### 再次复测（2026-09-08 19:45）

- 按用户要求不复用上一次结果，重新执行同一完整回归命令：12 个套件、146 项全部通过，退出码 0，耗时 30.999 秒。
- 使用新动态端口重新启动 home/local 后端 41456 和前端 41124；清除浏览器会话后通过验证码页面重新登录，并新建测试绘图 `355679770496729088`。
- 重新上传数据集、列属性和行属性文件；同一组 20 个 Playwright 场景连续运行两次均全部通过，其中第二次使用全新 trace 作为最终干净证据。
- 最终 trace 中动作/页面控制台错误为 0；42 次本地代理 API 请求无 HTTP 4xx/5xx。登录页恢复哈希一致且无新增 Git 差异，两项服务受控关闭，41124、41456 均已释放。

## 偏差、风险与遗留

此前外网 MySQL 间歇连接超时导致附加回归未完成，本次原命令完整通过；连接故障的具体网络/服务端原因未定位，不声称修复了网络。无未完成的当前验收项。初步 Playwright 编排中的定位器和动画等待问题已在测试脚本中调整，最终连续运行通过，未改产品实现。

trace 含会话请求信息，仅保存在本次项目外临时目录，不提交、不对外分发。前端提交只包含本次 8 个文件；工作区原有注释恢复、页面样式和管理端修改仍保持未暂存。本文档不记录自身提交哈希，通过 Change-Set trailer 发现归档提交。

## 关联资料

- [实施计划与完整验收记录](../../../../superpowers/goto-web/plans/2026-09-08-热图参数与标记联动调整.md)
- [Playwright 20 场景结果](/tmp/goto-heatmap-e2e-20260908-KimxWV/playwright-results.json)
- [连续交互脚本](/tmp/goto-heatmap-e2e-20260908-KimxWV/heatmap-interactions.js)
- 最终 trace：`/tmp/goto-heatmap-e2e-20260908-KimxWV/heatmap-e2e.trace.zip`；截图同目录。
- 再次复测证据：`/tmp/goto-heatmap-retest-20260908-EzcBNZ/playwright-results.json`、`/tmp/goto-heatmap-retest-20260908-EzcBNZ/heatmap-retest.trace.zip`；首次登录和准备阶段的 trace 独立保存，不计入最终干净 trace。
- 前端提交：`326cc1368e2e6547448355092efecb3c5af0d2ef`（`feat: 完善热图参数与标记联动`）。
- 本计划与完成总结由文档仓库同一归档提交收录；自身提交通过 Change-Set trailer 发现，不在正文自引用哈希。
