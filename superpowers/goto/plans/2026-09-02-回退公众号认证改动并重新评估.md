---
change_set_id: CS-20260902-b9106a66-f2a3-48b8-bc4c-a1b0215d4ea4
related_change_set_ids:
  - CS-20260902-a5d131af-1d35-4f77-95f9-b5db1d4b76e4
---

# 回退公众号认证改动并重新评估实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完整回退 `freedom-public` 提交 `81c7c26`，恢复 task 启动，并在停止继续改码的前提下重新评估 system 微信登录 Provider 的正确注册时机。

**Architecture:** 第一阶段只恢复提交 `81c7c26` 的父提交行为，不修改 goto-task 源码；以当前 task 启动失败作为 RED，以回退后的同一启动流程成功作为 GREEN。第二阶段只追踪 system 的认证管理器生命周期并提出候选方案，新的 system 修复必须再次经过设计确认。

**Tech Stack:** Java 17、Spring Boot 3.3.4、Spring Security 6、Maven、安全包装脚本、Git worktree。

## Global Constraints

- task 现在和未来都不提供登录能力，不新增 `PasswordEncoder`、登录接口或登录 Provider。
- 第一阶段只回退 `81c7c26` 的三个文件，不夹带 Redis、Hibernate、BeanPostProcessor 警告治理。
- Maven 只能使用 WSL 安全包装脚本，任何参数不得包含任意大小写形式的 `deploy`。
- Maven 命令必须保留实时完整输出，并同时写入独立日志。
- 本地启动使用绑定 `127.0.0.1:0` 得到的动态端口；启动完成或失败后必须关闭本次进程并确认端口释放。
- freedom-public 代码修改在 `/mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置` 隔离 worktree 中进行。
- 当前 Change-Set-Id 为 `CS-20260902-b9106a66-f2a3-48b8-bc4c-a1b0215d4ea4`，关联前序 `CS-20260902-a5d131af-1d35-4f77-95f9-b5db1d4b76e4`。

---

### Task 1: 建立隔离工作区并复现 task 启动失败

**Files:**

- Read: `/mnt/f/IdeaProjects/goto-software/freedom-public/common-security/src/main/java/com/freedom/config/WeChatOpenIdAuthenticationConfiguration.java`
- Read: `/mnt/f/IdeaProjects/goto-software/goto/task/src/main/java/com/freedom/config/TaskSecurityConfig.java`
- Build output: `/mnt/f/IdeaProjects/goto-software/goto/task/target/goto-task.jar`

**Interfaces:**

- Consumes: 当前共享仓库中的 `com.freedom:common-security:1.0`，其制品时间为本次回退前版本。
- Produces: 可重复的启动失败证据，错误必须是缺少 `PasswordEncoder`，而不是数据库或 Redis 连接失败。

- [ ] **Step 1: 使用 using-git-worktrees 创建 freedom-public 隔离工作区**

```bash
git -C /mnt/f/IdeaProjects/goto-software check-ignore -q worktrees
git -C /mnt/f/IdeaProjects/goto-software/freedom-public worktree add \
  /mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置 \
  -b fix/005-回退公众号认证配置
```

Expected: worktree 位于指定路径，分支为 `fix/005-回退公众号认证配置`，HEAD 为 `81c7c26`。

- [ ] **Step 2: 确认代码仓库没有混入用户变更**

```bash
git -C /mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置 status --short
git -C /mnt/f/IdeaProjects/goto-software/goto status --short
```

Expected: 新 worktree 干净；goto 仅保留任务开始前已存在的 `final-review-handoff.md` 修改。

- [ ] **Step 3: 使用当前共享制品构建 task 的 RED 启动包**

```bash
mkdir -p /tmp/goto-auth-rollback-logs
/mnt/g/Obsidian/code/01_System_Core/scripts/maven/run-wsl.sh \
  --project /mnt/f/IdeaProjects/goto-software/goto \
  --log /tmp/goto-auth-rollback-logs/task-red-package.log \
  -- -pl task -am clean package -DskipTests
```

Expected: `BUILD SUCCESS`，生成 `task/target/goto-task.jar`；构建成功只说明打包完成，不代表应用能启动。

- [ ] **Step 4: 在动态端口启动当前 task 并确认 RED**

```bash
TASK_RED_PORT="$(python3 -c 'import socket; s=socket.socket(); s.bind(("127.0.0.1", 0)); print(s.getsockname()[1]); s.close()')"
java -Dspring.profiles.active=home \
  -jar /mnt/f/IdeaProjects/goto-software/goto/task/target/goto-task.jar \
  --server.port="$TASK_RED_PORT"
```

Expected: 进程以非零状态退出，日志包含 `No qualifying bean of type 'org.springframework.security.crypto.password.PasswordEncoder'` 和 `APPLICATION FAILED TO START`。

- [ ] **Step 5: 确认 RED 进程没有残留监听端口**

```bash
lsof -nP -iTCP:"$TASK_RED_PORT" -sTCP:LISTEN
```

Expected: 无输出。

### Task 2: 完整回退 `81c7c26`

**Files:**

- Restore: `common-security/src/main/java/com/freedom/runner/SecurityRunner.java`
- Delete: `common-security/src/main/java/com/freedom/config/WeChatOpenIdAuthenticationConfiguration.java`
- Delete: `common-security/src/test/java/com/freedom/runner/SecurityRunnerTest.java`

**Interfaces:**

- Consumes: `AuthenticationManagerBuilder` 和 `WeChatOpenIdUserDetailsService`。
- Produces: 与 `81c7c26^` 完全一致的 `SecurityRunner` 启动后注册行为，不产生 `PasswordEncoder` 依赖。

- [ ] **Step 1: 运行提交级反向变更但暂不提交**

```bash
git -C /mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置 \
  revert --no-commit 81c7c26
```

Expected: 只出现上述三个文件的反向变更。

- [ ] **Step 2: 核对恢复后的生产代码内容**

```java
package com.freedom.runner;

import com.freedom.security.WechatOpenIdUserDetailsAuthenticationProvider;
import com.freedom.service.WeChatOpenIdUserDetailsService;
import lombok.RequiredArgsConstructor;
import org.springframework.boot.CommandLineRunner;
import org.springframework.security.authentication.ProviderManager;
import org.springframework.security.config.annotation.authentication.builders.AuthenticationManagerBuilder;
import org.springframework.stereotype.Component;

@Component
@RequiredArgsConstructor
public class SecurityRunner implements CommandLineRunner {
    private final AuthenticationManagerBuilder authenticationManagerBuilder;
    private final WeChatOpenIdUserDetailsService userDetailsService;

    @Override
    public void run(String... args) {
        ProviderManager providerManager = (ProviderManager) authenticationManagerBuilder.getObject();
        WechatOpenIdUserDetailsAuthenticationProvider authenticationProvider = new WechatOpenIdUserDetailsAuthenticationProvider();
        authenticationProvider.setUserDetailsService(userDetailsService);
        providerManager.getProviders().add(authenticationProvider);
    }
}
```

Run:

```bash
git -C /mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置 diff --check
git -C /mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置 diff --stat
git -C /mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置 diff 81c7c26^ -- \
  common-security/src/main/java/com/freedom/runner/SecurityRunner.java \
  common-security/src/main/java/com/freedom/config/WeChatOpenIdAuthenticationConfiguration.java \
  common-security/src/test/java/com/freedom/runner/SecurityRunnerTest.java
```

Expected: `git diff --check` 无输出；与 `81c7c26^` 比较无差异。

- [ ] **Step 3: 编译测试受影响的 common-security reactor**

```bash
/mnt/g/Obsidian/code/01_System_Core/scripts/maven/run-wsl.sh \
  --project /mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置 \
  --log /tmp/goto-auth-rollback-logs/freedom-common-security-test.log \
  -- -pl common-security -am clean install
```

Expected: `BUILD SUCCESS`，所有测试 `failures=0`、`errors=0`，日志文件完整存在。

- [ ] **Step 4: 备份当前共享制品并安装回退后的 common-security**

```bash
ROLLBACK_ARTIFACT_BACKUP="$(mktemp -d /tmp/goto-common-security-before-rollback.XXXXXX)"
cp /mnt/e/repository/com/freedom/common-security/1.0/common-security-1.0.jar \
  "$ROLLBACK_ARTIFACT_BACKUP/common-security-1.0.jar"
/mnt/g/Obsidian/code/01_System_Core/scripts/maven/run-wsl.sh \
  --project /mnt/f/IdeaProjects/goto-software/goto \
  --log /tmp/goto-auth-rollback-logs/install-reverted-common-security.log \
  -- org.apache.maven.plugins:maven-install-plugin:3.1.3:install-file \
  -Dfile=/mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置/common-security/target/common-security-1.0.jar \
  -DpomFile=/mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置/common-security/pom.xml
```

Expected: `BUILD SUCCESS`；备份目录保留到 task GREEN 验证结束，失败时用同一 `install-file` 命令恢复备份 JAR。

### Task 3: 验证 task 恢复并提交回退

**Files:**

- Verify: `/mnt/f/IdeaProjects/goto-software/goto/task/target/goto-task.jar`
- Commit: freedom-public 上述三个回退文件

**Interfaces:**

- Consumes: 回退后的 `common-security-1.0.jar`。
- Produces: 可启动且可受控关闭的 `goto-task.jar`，以及只包含提交反向变更的 Git commit。

- [ ] **Step 1: 完整编译测试 goto reactor**

```bash
/mnt/g/Obsidian/code/01_System_Core/scripts/maven/run-wsl.sh \
  --project /mnt/f/IdeaProjects/goto-software/goto \
  --log /tmp/goto-auth-rollback-logs/goto-clean-install.log \
  -- clean install
```

Expected: `BUILD SUCCESS`，所有测试 `failures=0`、`errors=0`，task JAR 重新生成。

- [ ] **Step 2: 在新动态端口启动 task 并确认 GREEN**

```bash
TASK_GREEN_PORT="$(python3 -c 'import socket; s=socket.socket(); s.bind(("127.0.0.1", 0)); print(s.getsockname()[1]); s.close()')"
java -Dspring.profiles.active=home \
  -jar /mnt/f/IdeaProjects/goto-software/goto/task/target/goto-task.jar \
  --server.port="$TASK_GREEN_PORT"
```

Expected: 日志包含 `Started TaskApplication`，且不包含 `Error creating bean`、`APPLICATION FAILED TO START` 或缺少 `PasswordEncoder`。

- [ ] **Step 3: 通过前台终端发送 Ctrl-C 并确认端口释放**

```text
向启动 task 的统一终端会话发送 Ctrl-C，等待 Java 进程退出。
```

Run:

```bash
lsof -nP -iTCP:"$TASK_GREEN_PORT" -sTCP:LISTEN
```

Expected: 无输出；本次启动的应用已经关闭。

- [ ] **Step 4: 完整验证 freedom-public reactor**

```bash
/mnt/g/Obsidian/code/01_System_Core/scripts/maven/run-wsl.sh \
  --project /mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置 \
  --log /tmp/goto-auth-rollback-logs/freedom-clean-install.log \
  -- clean install
```

Expected: `BUILD SUCCESS`，所有测试 `failures=0`、`errors=0`。

- [ ] **Step 5: 提交且只提交完整回退**

```bash
git -C /mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置 add \
  common-security/src/main/java/com/freedom/runner/SecurityRunner.java \
  common-security/src/main/java/com/freedom/config/WeChatOpenIdAuthenticationConfiguration.java \
  common-security/src/test/java/com/freedom/runner/SecurityRunnerTest.java
git -C /mnt/f/IdeaProjects/goto-software/worktrees/freedom-public/fix/005-回退公众号认证配置 \
  commit -m "revert: 回退公众号认证构建阶段注册" \
  -m "恢复 task 不依赖 PasswordEncoder 的启动行为。" \
  -m "Change-Set-Id: CS-20260902-b9106a66-f2a3-48b8-bc4c-a1b0215d4ea4" \
  -m "Related-Change-Set-Id: CS-20260902-a5d131af-1d35-4f77-95f9-b5db1d4b76e4"
```

Expected: 提交只包含三个反向变更文件；goto 仓库没有新增 Git 修改。

### Task 4: 重新评估 system 微信登录 Provider 注册时机

**Files:**

- Read: `common-security/src/main/java/com/freedom/runner/SecurityRunner.java`
- Read: `eladmin-system/src/main/java/com/freedom/config/SecurityConfig.java`
- Read: `eladmin-system/src/main/java/com/freedom/modules/system/rest/AuthorizationController.java`
- Read: Spring Security 6 的 `AuthenticationConfiguration` 与 `AuthenticationManagerBuilder` 实际版本源码

**Interfaces:**

- Consumes: 回退后的真实 system 认证装配链路。
- Produces: 一份面向用户的根因结论与 2 至 3 个候选方案；不产生 Java 源码变更。

- [ ] **Step 1: 反向追踪认证管理器取得与 Provider 注册顺序**

```bash
rg -n -C 5 \
  'AuthenticationManager|AuthenticationManagerBuilder|SecurityRunner|WeChatOpenIdAuthenticationToken|authenticationProvider' \
  common-security/src/main/java eladmin-system/src/main/java
```

Expected: 明确 `AuthorizationController` 获取认证管理器、`SecurityRunner` 执行以及 `SecurityConfig` 创建认证 Bean 的先后依赖。

- [ ] **Step 2: 对比三个最小候选方案**

逐项比较以下方案：

1. 在 `eladmin-system.SecurityConfig` 中直接注册 OpenId Provider 与账号密码 Provider。
2. 在 `eladmin-system` 中显式导入一个仅属于登录应用的 Provider 配置类。
3. 将两个 `AuthenticationProvider` 定义为 system 范围 Bean，由 Spring Security 标准装配链收集。

每个方案只按四项判断：注册时机、task 隔离、账号密码兼容、可测试性。不得增加配置开关、模块或自动配置框架。

- [ ] **Step 3: 向用户提交重新评估结果并停止实施**

Expected: 给出推荐方案、拒绝另外两种方案的具体理由、需要新增的最小失败测试和验收命令；明确 system 第二版修复尚未实施，等待用户确认新设计。
