# GOTO R 包批量重装扩展实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不实际重装容器内 R 包的前提下，为 `gotoStat` 增加独立安装与移除脚本，将三包批量入口改为通用名称，并补齐挂载与自动化测试。

**Architecture:** 六个 Rscript 文件分别负责 `gotoBase`、`gotoTools`、`gotoStat` 的单包安装或移除；一个 Bash 入口负责全部预检、按依赖顺序调用单包脚本及最终验证。WSL 容器启动脚本只负责把三个源码包挂载到固定容器路径，批量入口不复制源码、不重启容器。

**Tech Stack:** Bash、Docker CLI、Rscript、R `remotes` 包、基于伪 Docker 的 Bash 契约测试。

## Global Constraints

- 实现仓库：`/mnt/f/IdeaProjects/goto-software/goto-deploy-backend`。
- 设计依据：`/mnt/f/IdeaProjects/goto-software/goto-docs/superpowers/goto-deploy-backend/superpowers/specs/2026-08-19-goto-R包批量重装扩展设计.md`。
- 只保留一个批量入口：`.bash/reinstall_goto_r_packages.sh`。
- 六个单包脚本必须保持独立：`install/remove_gotoBase.sh`、`install/remove_gotoTools.sh`、`install/remove_gotoStat.sh`。
- 依赖关系：`gotoStat -> gotoTools -> gotoBase`。
- 移除顺序：`gotoStat -> gotoTools -> gotoBase`。
- 安装及最终验证顺序：`gotoBase -> gotoTools -> gotoStat`。
- 所有预检必须在首次移除前完成；任一步失败立即停止并传递非零退出码。
- 三个安装脚本统一使用 `force = TRUE`、`upgrade = "never"`、`dependencies = FALSE`。
- 不运行 `roxygen2::roxygenize()`，不修改三个 R 包业务代码，不升级依赖，不自动回滚。
- 本次不重建或重启 `goto_rbase_wsl`，不执行真实三包重装。
- 保留实现仓库现有未跟踪目录 `.bash/backup_old/` 和 `.idea/`，不得纳入提交。
- Git 提交信息使用中文；每次提交只包含任务列出的文件。

---

## File Map

| 文件 | 操作 | 职责 |
| --- | --- | --- |
| `.bash/install_gotoStat.sh` | 新增 | 从固定挂载路径安装 `gotoStat` |
| `.bash/remove_gotoStat.sh` | 新增 | 从所有 R 库幂等移除 `gotoStat` |
| `.bash/wsl_rbase_start.sh` | 修改 | 检查并挂载 `gotoStat` 源码目录 |
| `tests/bash/test_r_package_scripts.sh` | 修改 | 检查六个单包脚本与 `gotoStat` 挂载契约 |
| `.bash/reinstall_goto_r_packages.sh` | 由旧入口更名并修改 | 预检、编排三包移除/安装、最终验证 |
| `.bash/reinstall_gotoBase_gotoTools.sh` | 删除 | 移除不再准确的旧入口名 |
| `tests/bash/test_reinstall_goto_r_packages.sh` | 由旧测试更名并修改 | 验证通用入口顺序、参数、预检和快速失败 |
| `tests/bash/test_reinstall_gotoBase_gotoTools.sh` | 删除 | 移除与旧入口绑定的测试名 |

---

### Task 1: 增加 gotoStat 单包脚本和容器挂载

**Files:**

- Create: `.bash/install_gotoStat.sh`
- Create: `.bash/remove_gotoStat.sh`
- Modify: `.bash/wsl_rbase_start.sh:12-17,36-58,74-78`
- Modify: `tests/bash/test_r_package_scripts.sh:1-38`

**Interfaces:**

- Consumes: 宿主机源码目录 `/mnt/f/IdeaProjects/goto-software/gotoStat`、容器固定路径 `/root/software/gotoStat`、容器中的 `remotes`。
- Produces: 可由 `Rscript --vanilla /dev/stdin` 独立执行的 `install_gotoStat.sh`、`remove_gotoStat.sh`，以及未来重建容器时可用的 `gotoStat` bind mount。

- [ ] **Step 1: 扩展静态契约测试，使 gotoStat 缺失时失败**

将 `tests/bash/test_r_package_scripts.sh` 完整替换为：

```bash
#!/usr/bin/env bash
set -Eeuo pipefail
readonly TEST_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
readonly BASH_DIR="$(cd -- "$TEST_DIR/../../.bash" && pwd -P)"
readonly START_SCRIPT="$BASH_DIR/wsl_rbase_start.sh"
fail() { printf '测试失败：%s\n' "$1" >&2; exit 1; }
has() { grep -Fq -- "$2" "$1" || fail "$1 缺少：$2"; }
lacks() { ! grep -Fq -- "$2" "$1" || fail "$1 不应包含：$2"; }

check_install() {
    local file="$1" package="$2" path="$3"
    [[ -f "$file" ]] || fail "文件不存在：$file"
    [[ "$(sed -n '1p' "$file")" == '#!/usr/bin/env Rscript' ]] || fail "$file shebang 错误"
    has "$file" "package_name <- \"$package\""
    has "$file" "package_path <- \"$path\""
    has "$file" 'remotes::install_local('
    has "$file" 'force = TRUE'
    has "$file" 'upgrade = "never"'
    has "$file" 'dependencies = FALSE'
    lacks "$file" 'roxygen2::roxygenize'
    lacks "$file" 'remove.packages'
}

check_remove() {
    local file="$1" package="$2"
    [[ -f "$file" ]] || fail "文件不存在：$file"
    [[ "$(sed -n '1p' "$file")" == '#!/usr/bin/env Rscript' ]] || fail "$file shebang 错误"
    has "$file" "package_name <- \"$package\""
    has "$file" '.libPaths()'
    has "$file" 'remove.packages('
    has "$file" 'remaining_libraries'
    has "$file" 'quit(save = "no", status = 0)'
}

check_install "$BASH_DIR/install_gotoBase.sh" gotoBase /root/software/gotoBase
check_install "$BASH_DIR/install_gotoTools.sh" gotoTools /root/software/gotoTools
check_install "$BASH_DIR/install_gotoStat.sh" gotoStat /root/software/gotoStat
check_remove "$BASH_DIR/remove_gotoBase.sh" gotoBase
check_remove "$BASH_DIR/remove_gotoTools.sh" gotoTools
check_remove "$BASH_DIR/remove_gotoStat.sh" gotoStat

[[ -f "$START_SCRIPT" ]] || fail "容器启动脚本不存在：$START_SCRIPT"
has "$START_SCRIPT" 'LOCAL_GOTO_STAT_PATH="/mnt/f/IdeaProjects/goto-software/gotoStat"'
has "$START_SCRIPT" '-v "$LOCAL_GOTO_STAT_PATH:/root/software/gotoStat"'
printf '通过：六个单包 Rscript 与 gotoStat 挂载满足静态约束。\n'
```

- [ ] **Step 2: 运行测试并确认按预期失败**

Run:

```bash
cd /mnt/f/IdeaProjects/goto-software/goto-deploy-backend
tests/bash/test_r_package_scripts.sh
```

Expected: 退出码非零，输出包含：

```text
测试失败：文件不存在：.../.bash/install_gotoStat.sh
```

- [ ] **Step 3: 新增 gotoStat 安装脚本**

创建 `.bash/install_gotoStat.sh`：

```r
#!/usr/bin/env Rscript

# 只安装固定挂载路径中的 gotoStat；批量入口必须先安装 gotoBase 和 gotoTools。
package_name <- "gotoStat"
package_path <- "/root/software/gotoStat"
description_path <- file.path(package_path, "DESCRIPTION")
if (!file.exists(description_path)) {
    stop(sprintf("未找到 %s 源码描述文件：%s", package_name, description_path))
}
actual_package_name <- unname(read.dcf(description_path)[1, "Package"])
if (!identical(actual_package_name, package_name)) {
    stop(sprintf("源码包名不匹配：期望 %s，实际 %s", package_name, actual_package_name))
}
if (!requireNamespace("remotes", quietly = TRUE)) {
    stop("容器内未安装 remotes，无法安装本地 R 包")
}
message(sprintf("开始从 %s 安装 %s", package_path, package_name))
remotes::install_local(
    path = package_path,
    force = TRUE,
    upgrade = "never",
    dependencies = FALSE
)
message(sprintf("%s 安装成功", package_name))
tryCatch(
  message(sprintf("%s 版本：%s", package_name, as.character(utils::packageVersion(package_name)))),
  error = function(e) message(sprintf("安装成功但无法读取版本号：%s", conditionMessage(e)))
)
```

- [ ] **Step 4: 新增 gotoStat 幂等移除脚本**

创建 `.bash/remove_gotoStat.sh`：

```r
#!/usr/bin/env Rscript

# 移除所有 R 库目录中的 gotoStat；原本不存在时正常返回。
package_name <- "gotoStat"
find_package_libraries <- function() {
    .libPaths()[vapply(.libPaths(), function(library_path) {
        dir.exists(file.path(library_path, package_name))
    }, logical(1))]
}
installed_libraries <- find_package_libraries()
if (length(installed_libraries) == 0) {
    message(sprintf("%s 未安装，跳过移除", package_name))
    quit(save = "no", status = 0)
}
for (library_path in installed_libraries) {
    message(sprintf("正在从 %s 移除 %s", library_path, package_name))
    utils::remove.packages(package_name, lib = library_path)
}
remaining_libraries <- find_package_libraries()
if (length(remaining_libraries) > 0) {
    stop(sprintf("%s 移除不完整，仍存在于：%s", package_name,
                 paste(remaining_libraries, collapse = ", ")))
}
message(sprintf("%s 已完全移除", package_name))
```

- [ ] **Step 5: 给容器启动脚本增加 gotoStat 挂载**

在 `.bash/wsl_rbase_start.sh` 的本地路径区加入：

```bash
LOCAL_GOTO_STAT_PATH="/mnt/f/IdeaProjects/goto-software/gotoStat"
```

在 `LOCAL_GOTO_BASE_PATH` 检查之后加入：

```bash
if [ ! -d "$LOCAL_GOTO_STAT_PATH" ]; then
    echo "Warning: $LOCAL_GOTO_STAT_PATH does not exist, creating..."
    mkdir -p "$LOCAL_GOTO_STAT_PATH"
fi
```

在 `docker run` 的源码挂载区加入：

```bash
    -v "$LOCAL_GOTO_STAT_PATH:/root/software/gotoStat" \
```

- [ ] **Step 6: 设置执行权限并运行 Task 1 测试**

Run:

```bash
cd /mnt/f/IdeaProjects/goto-software/goto-deploy-backend
chmod 0755 .bash/install_gotoStat.sh .bash/remove_gotoStat.sh
tests/bash/test_r_package_scripts.sh
bash -n .bash/wsl_rbase_start.sh tests/bash/test_r_package_scripts.sh
git diff --check
```

Expected:

```text
通过：六个单包 Rscript 与 gotoStat 挂载满足静态约束。
```

`bash -n` 与 `git diff --check` 均以退出码 0 结束且无错误输出。

- [ ] **Step 7: 提交 Task 1**

Run:

```bash
git add -- .bash/install_gotoStat.sh .bash/remove_gotoStat.sh \
  .bash/wsl_rbase_start.sh tests/bash/test_r_package_scripts.sh
git diff --cached --check
git commit -m "feat: 增加gotoStat单包脚本与容器挂载"
```

Expected: 提交只包含上述四个文件，不包含 `.bash/backup_old/` 或 `.idea/`。

---

### Task 2: 更名并扩展三包批量入口

**Files:**

- Create: `.bash/reinstall_goto_r_packages.sh`
- Delete: `.bash/reinstall_gotoBase_gotoTools.sh`
- Create: `tests/bash/test_reinstall_goto_r_packages.sh`
- Delete: `tests/bash/test_reinstall_gotoBase_gotoTools.sh`

**Interfaces:**

- Consumes: Task 1 产出的 `install_gotoStat.sh`、`remove_gotoStat.sh`，现有四个 Base/Tools 单包脚本，以及零个或一个容器名参数。
- Produces: 唯一批量入口 `.bash/reinstall_goto_r_packages.sh [container_name]`；成功返回 0，参数、预检、步骤或最终验证失败返回非零。

- [ ] **Step 1: 先创建新入口的失败行为测试**

创建 `tests/bash/test_reinstall_goto_r_packages.sh`：

```bash
#!/usr/bin/env bash
set -Eeuo pipefail
readonly TEST_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
readonly BATCH="$TEST_DIR/../../.bash/reinstall_goto_r_packages.sh"
readonly OLD_BATCH="$TEST_DIR/../../.bash/reinstall_gotoBase_gotoTools.sh"
readonly TEMP_DIR="$(mktemp -d)"
FAKE_BIN="$TEMP_DIR/bin"
FAKE_LOG="$TEMP_DIR/docker.log"
trap 'rm -rf -- "$TEMP_DIR"' EXIT
fail() { printf '测试失败：%s\n' "$1" >&2; exit 1; }

mkdir -p "$FAKE_BIN"
cat > "$FAKE_BIN/docker" <<'FAKE'
#!/usr/bin/env bash
set -Eeuo pipefail
case "${1:-}" in
  inspect)
    printf 'INSPECT:%s\n' "${@: -1}" >> "$FAKE_LOG"
    [[ "${FAKE_STATE:-running}" != missing ]] || exit 1
    [[ "${FAKE_STATE:-running}" != stopped ]] && printf 'true\n' || printf 'false\n'
    ;;
  exec)
    shift
    stdin=false
    if [[ "${1:-}" == -i ]]; then stdin=true; shift; fi
    printf 'CONTAINER:%s\n' "${1:-}" >> "$FAKE_LOG"
    shift
    $stdin || exit 0
    payload="$(cat)"
    if [[ "$payload" == *'check_package_source <- function'* && "$payload" == *'"gotoStat"'* ]]; then step=preflight
    elif [[ "$payload" == *'package_names <- c("gotoBase", "gotoTools", "gotoStat")'* ]]; then step=verify
    elif [[ "$payload" == *'install_local('* && "$payload" == *'"gotoBase"'* ]]; then step=install_gotoBase
    elif [[ "$payload" == *'install_local('* && "$payload" == *'"gotoTools"'* ]]; then step=install_gotoTools
    elif [[ "$payload" == *'install_local('* && "$payload" == *'"gotoStat"'* ]]; then step=install_gotoStat
    elif [[ "$payload" == *'remove.packages('* && "$payload" == *'"gotoBase"'* ]]; then step=remove_gotoBase
    elif [[ "$payload" == *'remove.packages('* && "$payload" == *'"gotoTools"'* ]]; then step=remove_gotoTools
    elif [[ "$payload" == *'remove.packages('* && "$payload" == *'"gotoStat"'* ]]; then step=remove_gotoStat
    else exit 9
    fi
    printf 'STEP:%s\n' "$step" >> "$FAKE_LOG"
    [[ "${FAKE_FAIL_STEP:-}" != "$step" ]] || exit 42
    ;;
  *) exit 10 ;;
esac
FAKE
chmod 0755 "$FAKE_BIN/docker"

run_batch() {
  PATH="$FAKE_BIN:$PATH" FAKE_LOG="$FAKE_LOG" \
    FAKE_STATE="${FAKE_STATE:-running}" FAKE_FAIL_STEP="${FAKE_FAIL_STEP:-}" \
    "$BATCH" "$@"
}

[[ -f "$BATCH" ]] || fail "批量入口不存在：$BATCH"
[[ ! -e "$OLD_BATCH" ]] || fail "旧批量入口仍然存在：$OLD_BATCH"
: > "$FAKE_LOG"
run_batch >/dev/null
actual="$(grep '^STEP:' "$FAKE_LOG")"
expected=$'STEP:preflight\nSTEP:remove_gotoStat\nSTEP:remove_gotoTools\nSTEP:remove_gotoBase\nSTEP:install_gotoBase\nSTEP:install_gotoTools\nSTEP:install_gotoStat\nSTEP:verify'
[[ "$actual" == "$expected" ]] || fail "执行顺序错误：$actual"
grep -Fq 'INSPECT:goto_rbase_wsl' "$FAKE_LOG" || fail '默认容器错误'

: > "$FAKE_LOG"
run_batch custom_rbase >/dev/null
grep -Fq 'INSPECT:custom_rbase' "$FAKE_LOG" || fail '覆盖容器未生效'

: > "$FAKE_LOG"
if FAKE_STATE=missing run_batch missing_rbase >/dev/null 2>&1; then
  fail '不存在容器不应成功'
fi
! grep -Fq 'STEP:' "$FAKE_LOG" || fail '预检失败后执行了 R 步骤'

: > "$FAKE_LOG"
if FAKE_FAIL_STEP=install_gotoTools run_batch >/dev/null 2>&1; then
  fail 'gotoTools 安装失败不应成功'
fi
actual="$(grep '^STEP:' "$FAKE_LOG")"
expected=$'STEP:preflight\nSTEP:remove_gotoStat\nSTEP:remove_gotoTools\nSTEP:remove_gotoBase\nSTEP:install_gotoBase\nSTEP:install_gotoTools'
[[ "$actual" == "$expected" ]] || fail "失败后仍继续：$actual"

: > "$FAKE_LOG"
if run_batch one two >/dev/null 2>&1; then fail '多余参数不应成功'; fi
[[ ! -s "$FAKE_LOG" ]] || fail '参数错误时不应调用 Docker'
printf '通过：三包批量入口参数、预检、顺序和快速失败正确。\n'
```

- [ ] **Step 2: 运行新测试并确认按预期失败**

Run:

```bash
cd /mnt/f/IdeaProjects/goto-software/goto-deploy-backend
chmod 0755 tests/bash/test_reinstall_goto_r_packages.sh
tests/bash/test_reinstall_goto_r_packages.sh
```

Expected: 退出码非零，输出包含：

```text
测试失败：批量入口不存在：.../.bash/reinstall_goto_r_packages.sh
```

- [ ] **Step 3: 创建唯一的通用批量入口**

创建 `.bash/reinstall_goto_r_packages.sh`：

```bash
#!/usr/bin/env bash
set -Eeuo pipefail
readonly SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
readonly DEFAULT_CONTAINER_NAME=goto_rbase_wsl
readonly R_SCRIPTS=(
  remove_gotoStat.sh
  remove_gotoTools.sh
  remove_gotoBase.sh
  install_gotoBase.sh
  install_gotoTools.sh
  install_gotoStat.sh
)

if (( $# > 1 )); then
  printf '用法：%s [容器名]\n' "$(basename -- "$0")" >&2
  exit 2
fi
if (( $# == 1 )) && [[ -z "$1" ]]; then
  printf '错误：容器名不能为空。\n' >&2
  exit 2
fi
readonly container_name="${1:-$DEFAULT_CONTAINER_NAME}"
current_step=初始化
report_error() {
  local exit_code=$?
  printf '失败：步骤"%s"返回退出码 %d。\n' "$current_step" "$exit_code" >&2
  exit "$exit_code"
}
trap report_error ERR

printf '目标容器：%s\n' "$container_name"
current_step='检查 Docker 命令'
command -v docker >/dev/null 2>&1 || {
  printf '错误：WSL 宿主机未找到 docker 命令。\n' >&2
  exit 1
}
current_step='检查单包脚本'
for script_name in "${R_SCRIPTS[@]}"; do
  [[ -r "$SCRIPT_DIR/$script_name" ]] || {
    printf '错误：单包脚本不存在或不可读：%s\n' "$SCRIPT_DIR/$script_name" >&2
    exit 1
  }
done
current_step='检查目标容器'
if ! running="$(docker inspect --format '{{.State.Running}}' "$container_name" 2>/dev/null)"; then
  printf '错误：目标容器不存在或 Docker 不可访问：%s\n' "$container_name" >&2
  exit 1
fi
[[ "$running" == true ]] || {
  printf '错误：目标容器未运行：%s\n' "$container_name" >&2
  exit 1
}
current_step='检查容器内 Rscript'
docker exec "$container_name" sh -c 'command -v Rscript >/dev/null 2>&1'

current_step='检查容器内 R 环境和源码'
docker exec -i "$container_name" Rscript --vanilla /dev/stdin <<'RSCRIPT'
check_package_source <- function(package_path, expected_package_name) {
  description_path <- file.path(package_path, "DESCRIPTION")
  if (!file.exists(description_path)) stop(sprintf("未找到源码描述文件：%s", description_path))
  actual_package_name <- unname(read.dcf(description_path)[1, "Package"])
  if (!identical(actual_package_name, expected_package_name)) {
    stop(sprintf("源码包名不匹配：期望 %s，实际 %s",
                 expected_package_name, actual_package_name))
  }
}
if (!requireNamespace("remotes", quietly = TRUE)) stop("容器内未安装 remotes")
check_package_source("/root/software/gotoBase", "gotoBase")
check_package_source("/root/software/gotoTools", "gotoTools")
check_package_source("/root/software/gotoStat", "gotoStat")
message("容器内 R 环境和源码预检通过")
RSCRIPT

run_r_script() {
  current_step="$1"
  printf '\n[%s] 开始\n' "$current_step"
  docker exec -i "$container_name" Rscript --vanilla /dev/stdin < "$SCRIPT_DIR/$2"
  printf '[%s] 成功\n' "$current_step"
}
run_r_script '移除 gotoStat' remove_gotoStat.sh
run_r_script '移除 gotoTools' remove_gotoTools.sh
run_r_script '移除 gotoBase' remove_gotoBase.sh
run_r_script '安装 gotoBase' install_gotoBase.sh
run_r_script '安装 gotoTools' install_gotoTools.sh
run_r_script '安装 gotoStat' install_gotoStat.sh

current_step='验证安装结果'
printf '\n[验证安装结果] 开始\n'
docker exec -i "$container_name" Rscript --vanilla /dev/stdin <<'RSCRIPT'
package_names <- c("gotoBase", "gotoTools", "gotoStat")
for (package_name in package_names) {
  if (!requireNamespace(package_name, quietly = TRUE)) stop(sprintf("无法加载 %s", package_name))
  message(sprintf("%s 加载成功，版本：%s", package_name,
                  as.character(utils::packageVersion(package_name))))
}
RSCRIPT
printf '[验证安装结果] 成功\n'
trap - ERR
printf '\n批量重装完成：gotoBase、gotoTools 与 gotoStat 均已验证。\n'
```

- [ ] **Step 4: 删除旧入口并使测试名称与新入口一致**

删除：

```text
.bash/reinstall_gotoBase_gotoTools.sh
tests/bash/test_reinstall_gotoBase_gotoTools.sh
```

保留且不得修改 Task 1 已通过测试的六个单包脚本。

- [ ] **Step 5: 设置权限并运行 Task 2 测试**

Run:

```bash
cd /mnt/f/IdeaProjects/goto-software/goto-deploy-backend
chmod 0755 .bash/reinstall_goto_r_packages.sh \
  tests/bash/test_reinstall_goto_r_packages.sh
tests/bash/test_reinstall_goto_r_packages.sh
tests/bash/test_r_package_scripts.sh
bash -n .bash/reinstall_goto_r_packages.sh \
  tests/bash/test_reinstall_goto_r_packages.sh
git diff --check
```

Expected:

```text
通过：三包批量入口参数、预检、顺序和快速失败正确。
通过：六个单包 Rscript 与 gotoStat 挂载满足静态约束。
```

`bash -n` 与 `git diff --check` 均以退出码 0 结束且无错误输出。

- [ ] **Step 6: 提交 Task 2**

Run:

```bash
git add -- .bash/reinstall_goto_r_packages.sh \
  .bash/reinstall_gotoBase_gotoTools.sh \
  tests/bash/test_reinstall_goto_r_packages.sh \
  tests/bash/test_reinstall_gotoBase_gotoTools.sh
git diff --cached --check
git commit -m "refactor: 扩展通用R包批量重装入口"
```

Expected: Git 将旧文件识别为更名加内容修改，提交不包含 `.bash/backup_old/` 或 `.idea/`。

---

### Task 3: 执行非破坏性最终验证

**Files:**

- Verify: `.bash/install_gotoBase.sh`
- Verify: `.bash/remove_gotoBase.sh`
- Verify: `.bash/install_gotoTools.sh`
- Verify: `.bash/remove_gotoTools.sh`
- Verify: `.bash/install_gotoStat.sh`
- Verify: `.bash/remove_gotoStat.sh`
- Verify: `.bash/reinstall_goto_r_packages.sh`
- Verify: `.bash/wsl_rbase_start.sh`
- Verify: `tests/bash/test_r_package_scripts.sh`
- Verify: `tests/bash/test_reinstall_goto_r_packages.sh`

**Interfaces:**

- Consumes: Task 1 和 Task 2 的两个提交。
- Produces: Bash 语法、R 语法、契约测试、格式和变更范围的最新验证证据；不产生容器安装副作用。

- [ ] **Step 1: 运行完整 Bash 语法和契约测试**

Run:

```bash
cd /mnt/f/IdeaProjects/goto-software/goto-deploy-backend
bash -n .bash/reinstall_goto_r_packages.sh .bash/wsl_rbase_start.sh \
  tests/bash/test_r_package_scripts.sh \
  tests/bash/test_reinstall_goto_r_packages.sh
tests/bash/test_r_package_scripts.sh
tests/bash/test_reinstall_goto_r_packages.sh
```

Expected: 两个测试分别输出一条 `通过：`，所有命令退出码为 0。

- [ ] **Step 2: 在当前容器中只解析六个 Rscript**

Run:

```bash
for script in \
  .bash/install_gotoBase.sh .bash/remove_gotoBase.sh \
  .bash/install_gotoTools.sh .bash/remove_gotoTools.sh \
  .bash/install_gotoStat.sh .bash/remove_gotoStat.sh; do
  docker exec -i goto_rbase_wsl Rscript --vanilla \
    -e 'invisible(parse(file = stdin()))' < "$script"
done
```

Expected: 六次解析均以退出码 0 结束。该命令只解析代码，不调用安装或移除函数。

- [ ] **Step 3: 检查格式、变更范围和最终提交**

Run:

```bash
git diff --check
git status --short --branch
git log -2 --oneline --decorate
git show --stat --oneline HEAD~1
git show --stat --oneline HEAD
```

Expected:

- `git diff --check` 无输出。
- `git status` 只保留任务开始前已有的未跟踪 `.bash/backup_old/` 与 `.idea/`。
- 最近两个提交分别只包含 Task 1 和 Task 2 列出的文件。
- 不运行 `.bash/reinstall_goto_r_packages.sh`，因为当前容器尚未获得 `gotoStat` 挂载。
