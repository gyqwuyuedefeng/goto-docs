# goto-docs

goto-software 的文档集中管理仓库，包含 Superpowers 文档与 Spec Kit 正式功能规范。

## 目录结构

- `superpowers/` — Superpowers 设计、计划、运行记录和全部历史文档
- `SpecKit/specs/` — Spec Kit 正式功能规范

goto-software 只有一个 Spec Kit 项目根，即本仓库的 `SpecKit/`。G 盘中的 `*_spec.md` 项目规则文件是规则的唯一真相源，应以其为准。
